{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v max >/dev/null 2>&1 || (echo 'Max CLI not found. Install with: npm install -g sortlist-max-cli' >&2 && exit 0)"
          }
        ]
      }
    ]
  }
}
